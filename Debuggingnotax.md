# Debugging Log

## Initial Python Errors Encountered
- Error 1: ...  File "C:\Users\setbo\Documents\TheoWAF\SingleA_\SingleA_Armageddon_Lab3\Uhuru_Degubbing\python_app.py", line 98
    else
        ^
SyntaxError: expected ':' means that the prospective debgugger must add a semicolon
- Error 2: , line 103
    def bad_function(x, y): after some sleuthing i realized that you need to add a semicolon after the close parenthesis, and they i ran the softwaare it seems to be some graphic calculator and i recieved the following output "Enter an upper limit to generate primes:"and aafter that will tell user the odd or even status of values as well as prime numbers in an array of numbers.
    ## Javascript errors encountered
    -at this point I must rectify several ts1005 typescript syntax errors.
    -the if statement on like 73  needed an extra equals sign if (a % 2 == 0){
    -the dummy function on line 146 needed a closing bracket}
    ## Index.html
    as written in uhurus doc there was an additional parenthesis needed here.-if (isAdult(ageComputed)) {
    console.log("Adult");
} else {
    console.log("Child");
}   
   Declaration or statement expected on line 62 actually needed a bracket before the else statement

## Diagnosis Steps
-Problems tab on terminal was my map of the terrain to notice syntax errors
-I had to install nodejs
- Checked ...
- Ran ...

## Code Changes Made
- File: `python_app.py` — changed X to Y because ...

## Final Result
Application runs successfully. Steps to run:
1. ...node script.js
2. ,,, python python_app.py
